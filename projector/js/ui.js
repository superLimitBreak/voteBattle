(function(external){
// -----------------------------------------------------------------------------

function ui_update_stats() {
    console.log("update_stats");
    
    var build_player_li = function(player) {
        
    };
    
    $characters = $('.characters');
    $.each(data.players, function(i, player){
        
    });
}

// External --------------------------------------------------------------------

external.ui_update_stats();

}(global));